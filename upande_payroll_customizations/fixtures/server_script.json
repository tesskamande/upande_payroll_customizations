[
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2025-11-18 22:09:43.559896",
  "module": null,
  "name": "Recurring Deductions",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Salary Slip",
  "script": "# Before Save Server Script on Salary Slip\n\n# Fetch Employee record\nemployee_doc = frappe.get_doc(\"Employee\", doc.employee)\n\n# Safely get recurring deductions (empty list if None)\nrecurring_deductions = employee_doc.get(\"custom_recurring_deductions\") or []\n\nperiod_start = frappe.utils.getdate(doc.start_date)\nperiod_end = frappe.utils.getdate(doc.end_date)\n\n# Track existing components in Salary Slip to avoid duplicates\nexisting_components = {d.salary_component for d in doc.deductions}\n\nfor row in recurring_deductions:\n    # Skip inactive deductions\n    if not row.get(\"is_active\"):\n        continue\n    \n    # Check if deduction is valid for this salary period\n    start_ok = not row.get(\"start_date\") or frappe.utils.getdate(row.start_date) <= period_end\n    end_ok = not row.get(\"end_date\") or frappe.utils.getdate(row.end_date) >= period_start\n    \n    if start_ok and end_ok:\n        component = row.get(\"deduction_type\")\n        \n        # Append if not already in Salary Slip\n        if component and component not in existing_components:\n            doc.append(\"deductions\", {\n                \"salary_component\": component,\n                \"amount\": row.get(\"amount\") or 0,\n                \"default_amount\": row.get(\"amount\") or 0\n            })\n            existing_components.add(component)\n\n# Recalculate total deductions including appended rows\ndoc.total_deduction = sum([d.amount for d in doc.deductions])\ndoc.net_pay = (doc.gross_pay or 0) - (doc.total_deduction or 0)\ndoc.rounded_total = round(doc.net_pay or 0)\n\n# Helper function to get first day of month (accepts both date objects and strings)\ndef get_first_day(date_input):\n    \"\"\"Convert date to first day of that month\"\"\"\n    # Convert to date object if it's a string\n    if isinstance(date_input, str):\n        date_obj = frappe.utils.getdate(date_input)\n    else:\n        date_obj = date_input\n    \n    # Return first day of the month\n    return frappe.utils.getdate(f\"{date_obj.year:04d}-{date_obj.month:02d}-01\")\n\nfirst_day_month = get_first_day(period_start)\n\nmonth_slips = frappe.get_all(\n    \"Salary Slip\",\n    filters={\n        \"employee\": doc.employee,\n        \"docstatus\": 1,\n        \"start_date\": [\">=\", first_day_month],\n        \"end_date\": [\"<=\", doc.end_date]\n    },\n    fields=[\"net_pay\"]\n)\n\ndoc.month_to_date = sum([s.net_pay for s in month_slips]) + (doc.net_pay or 0)\n\n# Get fiscal year start date\nfiscal_year = frappe.db.get_value(\"Fiscal Year\", {\"name\": doc.fiscal_year}, \"year_start_date\") if doc.fiscal_year else None\n\nif fiscal_year:\n    fiscal_year_start = frappe.utils.getdate(fiscal_year)\nelse:\n    # Fallback to January 1st of the year\n    fiscal_year_start = frappe.utils.getdate(f\"{period_start.year:04d}-01-01\")\n\nyear_slips = frappe.get_all(\n    \"Salary Slip\",\n    filters={\n        \"employee\": doc.employee,\n        \"docstatus\": 1,\n        \"start_date\": [\">=\", fiscal_year_start],\n        \"end_date\": [\"<=\", doc.end_date]\n    },\n    fields=[\"net_pay\"]\n)\n\ndoc.year_to_date = sum([s.net_pay for s in year_slips]) + (doc.net_pay or 0)",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2025-11-24 10:36:42.241251",
  "module": null,
  "name": "Employee_naming_change",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Employee",
  "script": "\nif doc.is_new():\n    # Check if employee_number exists\n    if not doc.employee_number:\n        frappe.throw(\"Employee Number is required\")\n    \n    # Clear the naming_series to prevent it from being used\n    doc.naming_series = None\n    \n    # Force set the name directly\n    doc.name = doc.employee_number\n    \n    # Set flags to bypass standard naming\n    doc.flags.name_set = True",
  "script_type": "DocType Event"
 }
]